# Optional: OSCAL schema validation via compliance-trestle (IBM, Apache 2.0)
# Install with: pip install compliance-trestle
# Enables the --validate flag in oscal_generate.py
compliance-trestle>=2.3.0

# Optional: human-readable OSCAL CLI viewer
# Install with: pip install oscal-cli  (if available via pip)
# Or download the NIST OSCAL CLI Java tool from:
#   https://github.com/usnistgov/oscal-cli/releases
